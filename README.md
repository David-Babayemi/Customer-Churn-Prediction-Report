# Customer-Churn-Prediction-Report
The model was trained on a dataset integrated from demographics, transaction history, online activity, and service interactions. A train-test split (80/20) and 3-fold cross-validation with grid search were used to optimize performance.
